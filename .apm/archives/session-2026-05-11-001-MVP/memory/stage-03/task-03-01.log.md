---
stage: 3
task: 1
title: Voice connection plumbing
domain: core
status: Success
important_findings: false
compatibility_issues: false
---

# Task 3.1 - Voice connection plumbing

## Summary
`app/voice.py` was created with the three required helpers (`connect`, `play_reverie`, `disconnect`) and `REVERIE_PATH`. Seven tests in `tests/test_voice.py` cover happy paths and exception propagation for all three functions; all five pipeline checks pass clean.

## Details
- Verified `assets/reverie.wav` exists in the worktree at `assets/reverie.wav`.
- Created `app/voice.py` with `REVERIE_PATH` resolved at import time via `(Path(__file__).parent / ".." / "assets" / "reverie.wav").resolve()`. The module docstring describes it as a shim so no other module needs to import `discord.FFmpegPCMAudio` directly.
- `connect` and `disconnect` are `async def`; `play_reverie` is synchronous (returns immediately, no wait for playback completion — composition deferred to Stage 4 as instructed).
- All three functions propagate exceptions without catching.
- Created `tests/test_voice.py` with seven tests:
  - `test_connect_calls_channel_connect_once` — happy path, verifies return value.
  - `test_connect_propagates_exception` — unhappy path.
  - `test_play_reverie_calls_play_once` — happy path, basic call count.
  - `test_play_reverie_uses_resolved_reverie_path` — verifies `FFmpegPCMAudio` is constructed with `str(REVERIE_PATH)`, that the path is absolute, and that `voice_client.play` receives the mock audio object. `discord.FFmpegPCMAudio` patched at `app.voice.discord.FFmpegPCMAudio` (patch-where-used).
  - `test_play_reverie_propagates_exception` — unhappy path.
  - `test_disconnect_calls_disconnect_once` — happy path.
  - `test_disconnect_propagates_exception` — unhappy path.
- One ruff lint error was caught on the first pass (`F841` unused variable in `test_play_reverie_calls_play_once`); fixed by removing the `as mock_ffmpeg` alias.
- The `.venv` lives in the project root, not the worktree; all pipeline commands were run using the absolute path `/home/jfang/WSL/github.com/jonathan-fang/teamode/.venv/bin/...`.

## Output
- `/home/jfang/WSL/github.com/jonathan-fang/teamode/.apm/worktrees/voice-plumbing/app/voice.py`
- `/home/jfang/WSL/github.com/jonathan-fang/teamode/.apm/worktrees/voice-plumbing/tests/test_voice.py`
- Committed on `feat/voice-plumbing` as `bcf6940`.

## Validation
1. `ruff format --check` — 14 files already formatted, clean.
2. `ruff check` — All checks passed (after fixing F841).
3. `pytest tests/ -v` — 63 passed, 1 deprecation warning (audioop in discord.py itself, not our code).
4. `pyright` — 0 errors, 0 warnings, 0 informations.
5. `scan_injection.sh .apm` — OK: No injection patterns found.

## Issues
None.
